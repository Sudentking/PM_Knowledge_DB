# 场景不同，测评方法需要因地制宜：最新摸索的测评“四象限法则”分享

**作者**: 
**发布时间**: 
**原文链接**: [https://www.woshipm.com/ai/6385790.html](https://www.woshipm.com/ai/6385790.html)
**标签**: 

---

好的，作为一名专业的产品经理知识总结专家，我将对您提供的文章进行详细总结。

---

### 核心观点（200字）

文章的核心观点是：AI产品的评测方法不能一成不变，必须根据业务场景的复杂性和价值进行“因地制宜”的动态调整。传统的“核心-边缘-高价值”三分法评测框架存在局限性，尤其是在处理“高价值低频”场景时容易失效，导致模型在评测集上表现良好，但在实际业务中却频繁出错。

为此，作者提出了“四象限法则”评测框架，将场景划分为**核心高频、高价值低频、边缘场景、可延后场景**四类，并针对每类场景提出了差异化的优化策略（归集、先拆后收、定向优化、忽略）。文章强调，评测集是一个“活的东西”，需要建立一套动态演进的机制，定期审视和调整场景分类，才能持续保障产品质量，避免技术债的累积。

### 方法论/框架（300字）

文章的核心方法论是**“四象限法则”评测框架**，它是对传统三分法的升级，核心在于对场景进行更精细的分类和动态管理。

**四象限分类与策略：**

1.  **核心高频场景（归集）**：业务量最大、用户最常问的问题（如“查销售额”）。策略是**归集**，即将相同意图的不同问法合并为一个评测单元，构建稳定的“基本盘”，要求准确率极高（如98%以上）。
2.  **高价值低频场景（先拆后收）**：出现次数少，但一旦出错会造成重大事故的场景（如“环比增长率”）。策略是**先拆后收**。首先将场景拆解为更细粒度的子场景（如“日环比”、“周环比”），分别训练和评测；待模型在每个子场景上达到一定准确率后，再尝试合并成一个泛化的能力。
3.  **边缘场景（定向优化）**：用户的各种奇葩问法（错别字、方言等）。策略是**定向优化**，即挑选出现频率最高的Top N个边缘类型集中解决，而非一次性全部处理，避免带偏核心模型。
4.  **可延后场景（忽略）**：出现次数极少，且答错不影响核心体验的问题。策略是**忽略**，直接转人工或兜底，避免评测集无限膨胀，影响迭代效率。

**动态演进机制：**
*   **定期跑分**：定期对四类场景的准确率进行监控。
*   **时机判断**：当模型在某个大类上准确率卡在70%-80%且后端业务动作有分支时，考虑“拆”；当子类准确率均超90%后，尝试“收”。
*   **跨类别切换**：关注场景是否因业务变化而跨类别（如从边缘变为核心），灵活应对。

### 关键案例（200字）

文章通过两个关键案例生动展示了新框架的应用效果：

1.  **智能客服-退款场景**：
    *   **问题**：传统方法将“退款”作为一个核心场景，导致模型无法区分“破损退款”和“漏发退款”这两种后端业务动作完全不同的子场景。
    *   **新框架应用**：首先将“退款”拆分为`refund_broke`、`refund_leak`、`refund_other`三个子类，分别标注和训练。待每个子类准确率达到90%后，再尝试合并为一个大的`refund`意图，通过槽位区分具体类型。这解决了模型“一刀切”的问题。

2.  **问数项目-环比场景**：
    *   **问题**：模型在处理“环比”时，只学会了“日环比”，导致“周环比”、“月环比”的计算完全错误，业务同学抱怨“用不了”。
    *   **新框架应用**：将“环比”拆解为“日环比”、“周环比”、“月环比”三个子场景，分别标注样本和训练。两周后，模型在每个子场景上都表现良好，再合并成一个泛化的“环比理解”能力，成功解决了模型理解片面的问题。

### 实践建议（200字）

1.  **拆分的边界**：只有**后端业务动作不同**的场景才需要拆分子类；如果只是用户表达差异，应使用同义问法覆盖，避免过度拆分导致维护成本爆炸。
2.  **归集是前提**：在“拆”之前，必须先完成“归集”，将相同意图的不同问法合并为一个评测单元，避免重复劳动。
3.  **管理“可延后”**：为“可延后场景”设定硬性门槛（如单月出现少于X次），并规定每个版本至少解决10个之前延后的场景，防止其变成技术债。
4.  **边缘场景优先级**：不要一次性处理所有边缘场景，应根据实际业务需求，优先解决频率最高的Top 5类型，集中资源定向优化。
5.  **动态调整**：评测集不是一成不变的，需要定期跑分，并根据业务变化和模型能力，动态调整场景的“拆”与“收”，以及场景在四象限中的归属。
6.  **数据驱动决策**：不要轻信业务方“都很重要”的说法，要用数据说话，敢于“丢弃”低价值场景，集中精力解决核心问题。

### 关键词标签

四象限法则，场景分类，AI评测，动态演进，归集与拆分，定向优化，高价值低频，产品经理

---

*本文由AI自动总结生成，仅供参考。*
