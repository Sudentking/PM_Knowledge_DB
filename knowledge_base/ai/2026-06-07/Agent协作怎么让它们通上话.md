# Agent协作：怎么让它们通上话

**作者**: 
**发布时间**: 
**原文链接**: [https://www.woshipm.com/ai/6408896.html](https://www.woshipm.com/ai/6408896.html)
**标签**: 

---

好的，作为一名专业的产品经理知识总结专家，我对您提供的文章进行了详细的分析和总结。以下是按照您要求的格式输出的内容。

---

## 核心观点（200字）

本文的核心观点是，实现多个AI Agent之间的自主协作，其首要且最关键的障碍并非复杂的Prompt设计，而是底层通信链路的打通。文章以Telegram平台为例，揭示了平台出于防止Bot死循环的考虑，故意设置了“Bot之间互不可见”的硬性限制，导致Agent无法直接对话。作者通过实践发现，利用Channel（广播频道）替代Group（群组）作为通信“信使”，可以绕过这一限制。然而，打通信道后，又面临了权限配置、配置持久化以及消息循环等新问题。文章最终强调，Agent协作的稳定性依赖于“硬约束”（代码层补丁）与“软约束”（Prompt规则）的结合，并警示了AI工具在缺乏全局观时可能带来的风险。

## 方法论/框架（300字）

文章在解决Agent通信问题的过程中，提炼出一套实用的“问题诊断与解决框架”，具体步骤如下：

1.  **问题定位与根因分析**：
    *   **现象**：Agent之间无法互相发送消息。
    *   **排查**：检查配置（Gateway日志、BotFather设置、Privacy Mode），确认非配置问题。
    *   **根因**：查阅官方FAQ，发现平台对Bot间通信的硬性限制（`bots will not be able to see messages from other bots`）。

2.  **方案探索与验证**：
    *   **思路**：寻找平台规则的“盲区”或“特例”。
    *   **发现**：借鉴开源项目（OpenClaw），发现Channel（广播场景）不受此限制，而Group（对话场景）受限。
    *   **方案**：创建私有Channel，将两个Bot都设为管理员，作为通信“信使”。

3.  **问题迭代与修复（坑点排查）**：
    *   **坑一：权限与配置持久化**：
        *   **问题**：Channel消息的User ID是Channel ID而非用户ID，不在白名单内，被Gateway拒绝。
        *   **解决**：将Channel ID加入白名单。
        *   **新问题**：手动修改`.env`文件，在Gateway重启后被覆盖。
        *   **最佳实践**：使用非侵入式配置方式（如`hermes config set`写入`config.yaml`），避免直接修改源码或易被覆盖的配置文件。
    *   **坑二：消息循环与软约束失效**：
        *   **问题**：Channel消息被视为DM，跳过了Group中的`require_mention`检查，导致Agent回复所有消息，形成死循环。
        *   **解决**：采用“硬约束+软约束”双层防护。
            *   **硬约束**：修改Gateway源码，将`channel`类型加入群聊判断逻辑（`is_group_chat`函数），使`require_mention`对Channel生效。
            *   **软约束**：在Prompt中设置四条规则（只回@消息、文本前缀辨身份、不带@结束对话、弹性轮次上限），作为模型行为的指导。

4.  **最终方案**：通过代码补丁（硬约束）确保基础行为正确，再通过Prompt（软约束）优化交互逻辑，并辅以作者签名和Emoji前缀等机制进行双重确认。

## 关键案例（200字）

文章的核心案例是作者需要让两个功能不同的Agent（“小虾子”负责写作采集，“小蚊子”负责GPU压制）实现协作。理想流程是“小虾子”在群组中@“小蚊子”并分配任务。

**失败案例**：直接在Telegram Group中尝试，发现Bot之间无法看到对方消息，协作流程在第一环节就彻底卡住。

**成功案例**：作者通过以下步骤成功打通了通信链路：
1.  **信道切换**：放弃Group，创建一个私有Channel作为通信“信使”。
2.  **权限修复**：将Channel ID加入Gateway的白名单，并采用`hermes config set`命令实现配置持久化，避免重启丢失。
3.  **循环修复**：发现Channel消息导致死循环后，通过修改一行代码（在`is_group_chat`函数中加入`channel`类型），为Channel消息也施加了“只回复@自己的消息”的硬性约束。
4.  **最终验证**：在硬约束生效的基础上，配合Prompt中的四条软规则，最终实现了两个Agent在Channel中稳定、有序的自主协作。

## 实践建议（200字）

1.  **优先打通通信链路**：在规划Agent协作时，应将底层通信可行性作为首要验证点，而非直接投入Prompt设计。通信是协作的“地基”。
2.  **遵循“非侵入式”配置原则**：修改系统配置时，优先使用官方提供的配置接口（如`hermes config set`），避免直接修改`.env`或源码等易被覆盖或更新的文件，确保配置的持久化和可沉淀性。
3.  **建立“硬约束”优于“软约束”的认知**：对于防止死循环、权限校验等关键行为，必须通过代码层面的“硬约束”来兜底。仅依赖Prompt的“软约束”是不可靠的，模型可能不遵守规则。
4.  **警惕AI工具的“局部最优”陷阱**：AI工具（如AI Coding Agent）倾向于搜索关键词并直接修改，缺乏对系统整体架构的理解。在使用时，应通过Prompt限制其行为，或依靠自身经验判断其操作的正确性，避免其做出“删库跑路”等危险操作。执行任何脚本前，务必进行Dry Run。

## 关键词标签
Agent协作, 通信链路, 硬约束, 软约束, 消息循环, 配置持久化, 非侵入式, Telegram Bot

---

*本文由AI自动总结生成，仅供参考。*
