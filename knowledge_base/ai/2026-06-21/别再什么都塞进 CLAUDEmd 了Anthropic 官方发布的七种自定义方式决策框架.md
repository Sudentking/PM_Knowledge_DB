# 别再什么都塞进 CLAUDE.md 了——Anthropic 官方发布的七种自定义方式决策框架

**作者**: 
**发布时间**: 
**原文链接**: [https://www.woshipm.com/ai/6416320.html](https://www.woshipm.com/ai/6416320.html)
**标签**: 

---

好的，作为一名专业的产品经理知识总结专家，我已仔细阅读并分析了您提供的文章。以下是对该文章的详细总结。

---

## 核心观点（200字）

本文的核心观点是：在配置AI开发助手（特别是Claude Code）时，**指令放置的位置比指令本身的内容更重要**。许多用户将所有自定义指令都塞入单一的`CLAUDE.md`文件，导致其变成一个臃肿的“垃圾桶”，不仅消耗宝贵的上下文Token，还稀释了关键信息，降低了AI助手的效率。

文章指出，Anthropic官方提供了七种不同的自定义行为机制，每种机制都有其特定的加载时机、压缩行为和上下文成本。正确理解和运用这些机制，将指令放置在最适合的位置，能够实现“按需加载”，精准分配上下文资源，从而显著提升AI助手的性能表现。核心在于将上下文窗口视为最昂贵的资源，并对其进行精细化管理。

## 方法论/框架（300字）

文章提供了一个名为 **“七种自定义方式决策框架”** 的方法论，核心是理解每种机制的三个关键差异：**加载时机**、**压缩行为**和**上下文成本**。基于此，文章将七种机制分为三类：

1.  **全局/常驻型（高成本）**：
    *   **根目录`CLAUDE.md`**：会话启动即加载，全程不释放，上下文成本最高。适合放置“项目宪法”，如构建命令、目录结构、团队编码约定等每次会话都用得上的核心事实。
    *   **输出风格（Output Styles）**：启动时注入，永不压缩，成本高。用于角色转换，如将Claude从编程助手改为通用助手。

2.  **按需/触发型（低成本）**：
    *   **子目录`CLAUDE.md`**：按需加载，仅在Claude处理该子目录文件时触发，压缩后释放。适合放置仅与特定模块相关的约定。
    *   **规则（Rules）**：核心创新在于`paths:`字段。未加路径的规则行为同`CLAUDE.md`；加了路径后，仅在匹配文件被触碰时加载，压缩后重新注入。适合处理跨目录的横切关注点（如“所有API handler必须用Zod校验”）。
    *   **技能（Skills）**：启动时仅加载名字和描述（约100 Token），技能体在调用时才加载。适合放置流程类指令，如部署清单、代码审查步骤。
    *   **子代理（Subagents）**：启动时加载名字、描述和工具列表，代理体在调用时加载，并在独立上下文中运行，仅返回最终结果。适合深度搜索、日志分析等中间过程不重要的任务。

3.  **事件/临时型（极低成本）**：
    *   **钩子（Hooks）**：基于生命周期事件触发，完全绕过压缩机制，配置本身不在主上下文。适合必须确定性发生的事件，如自动格式化、发送通知、阻断危险命令。
    *   **追加系统提示词**：通过CLI标志在调用时传入，仅对单次调用生效，不覆盖默认角色。适合临时调整语气或输出格式。

## 关键案例（200字）

文章通过一个反面案例揭示了问题的严重性：一个典型的`CLAUDE.md`文件变成了“垃圾桶”，里面混杂了构建命令、代码规范、部署流程、安全审查清单等所有内容。结果，无论任务是修复一个拼写错误还是重构整个认证模块，Claude每次启动都要从头到尾读取这个臃肿的文件，导致上下文窗口被无关信息挤满，关键信息被稀释，效率低下。

文章还提供了一个正面对比案例，展示了`Rules`机制中`paths:`字段的威力。例如，一条“所有API handler必须用Zod做输入校验”的规则，如果不加`paths`，会在编写CSS、配置Docker等无关任务时也占用Token。而加上`paths: [“src/api/**”]`后，这条规则只在Claude处理API代码时才进入上下文，实现了成本的精确分配。

## 实践建议（200字）

1.  **为`CLAUDE.md`瘦身**：将其严格限定为“项目宪法”，控制在200行以内，并指定专人负责，像审查代码一样审查其每一次变更。将部署步骤、安全检查清单等流程类内容移入`Skills`。
2.  **善用`Rules`的`paths:`字段**：对于跨模块的横切关注点，不要塞入`CLAUDE.md`，而是创建带路径限定的`Rules`，将上下文成本精确分配到有用的时候。
3.  **利用`Skills`管理流程**：将部署、代码审查、发布流程等“程序性”指令放入`Skills`。确保`Skills`的名字和描述足够精确，以便Claude能自动匹配或通过斜杠命令手动调用。
4.  **为复杂任务启用`Subagents`**：对于日志分析、依赖审计等中间过程噪音大、只需最终结果的任务，使用`Subagents`在独立上下文中运行，避免污染主会话。
5.  **使用`Hooks`处理确定性事件**：将文件编辑后自动格式化、完成后发送通知等必须发生的事件交给`Hooks`，它们不占用主上下文。
6.  **在Monorepo中利用子目录`CLAUDE.md`**：让每个团队或模块维护自己的子目录`CLAUDE.md`，实现按需加载，避免前端为后端规范买单。

## 关键词标签

CLAUDE.md, 上下文窗口, 决策框架, 按需加载, Rules, Skills, Subagents, Hooks

---

*本文由AI自动总结生成，仅供参考。*
