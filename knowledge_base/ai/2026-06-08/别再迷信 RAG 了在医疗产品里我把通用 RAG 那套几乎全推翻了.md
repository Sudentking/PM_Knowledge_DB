# 别再迷信 RAG 了：在医疗产品里，我把通用 RAG 那套几乎全推翻了

**作者**: 
**发布时间**: 
**原文链接**: [https://www.woshipm.com/ai/6409257.html](https://www.woshipm.com/ai/6409257.html)
**标签**: 

---

好的，作为一名专业的产品经理知识总结专家，我将对这篇文章进行详细总结。

---

### 核心观点（200字）

本文的核心观点是：在高风险场景（如医疗分诊）下，不能盲目迷信和照搬通用RAG（检索增强生成）技术。通用RAG的设计前提是用户为“会兜底的专家”（如医生），其核心逻辑是“求共识、找平衡、不确定则转人工”。然而，当使用者变为“会全信的素人”（如患者家属）时，这些逻辑会带来致命风险。文章通过一个夜间AI健康助手的案例，系统性地推翻了通用RAG的融合策略、评测指标、兜底逻辑和推荐逻辑，提出了一套以“零漏判”为最高优先级、以“取严取高”为融合原则、以“默认最坏”为兜底策略的全新设计范式。核心结论是：技术方案必须服务于业务场景的“输错代价”，代价不同，方案逻辑可以完全相反。

### 方法论/框架（300字）

文章提出了一套针对高风险场景的“反通用RAG”方法论框架，核心是四个“一刀”：

1.  **融合策略：取严取高，而非求共识**
    *   **通用做法**：RRF融合、投票取共识，旨在平衡召回率与准确率，降低误报。
    *   **高风险做法**：规则路与语义路双路并行，任一路判定为最高风险等级（如L1危急），最终结果就取该等级。核心逻辑是“不怕误报，只怕漏报”，用可容忍的误报率去死保零漏判。

2.  **评测指标：死磕单一红线，而非追求均衡**
    *   **通用做法**：同时关注Recall、Precision、NDCG等指标，追求均衡。
    *   **高风险做法**：只设定一条红线——L1漏判率（医学上称敏感度）必须无限逼近0。将误报率降级为“成本指标”，设定软上限（不淹没人工、不破坏信任），主动为“零漏判”牺牲。同时，标准答案的构建需注意两个反直觉点：历史工单的结论需经医生重标，不能直接使用；需由专家构造覆盖罕见但致命疾病的对抗性测试用例。

3.  **兜底逻辑：默认最坏，而非转人工**
    *   **通用做法**：AI拿不准时，输出“不确定，转人工”。
    *   **高风险做法**：将“不确定”视为高危信号。当双路无结论、超时或信息不足时，默认按最高风险等级（L1）处理，立即触发120报警和告警。转人工必须与120并行，绝不能替代，避免因等待人工而延误黄金救援时间。

4.  **推荐逻辑：硬约束过滤 + 业务规则置顶，而非纯加权排序**
    *   **通用做法**：向量召回后，根据多个维度加权排序，推荐最优结果。
    *   **高风险做法**：将推荐拆解为两步。第一步是“硬约束过滤”，将“医生不出诊”、“科室不对”、“权益不够”等条件作为一票否决的过滤器，而非扣分项。第二步是“业务规则置顶”，对于“用户长期看诊的医生”等体现“懂你、连续”价值的业务规则，使用置顶规则（如直接置顶），而非简单的加分项，确保核心体验不被其他维度淹没。

### 关键案例（200字）

文章的核心案例是“夜间AI健康助手”处理一条“我妈胸口闷了一晚上，有点喘不上气，要不明天帮我约个心内科吧？”的消息。

*   **通用RAG的致命错误**：标准RAG会将其理解为“预约明天心内科”，并给出预约建议。但实际这是76岁、有冠心病史的老太太的急性冠脉综合征（心梗）前兆，需要立即打120。这个案例生动展示了“输错代价”的极端性——回错一句话会死人。
*   **“红旗症状库”的构建**：为解决漏判，文章引入了三级红旗症状库（L1危急、L2紧急、L3普通）。但难点在于如何判断。关键词匹配（BM25）会因口语化表达（如“胸口闷” vs “胸痛”）而漏判；纯语义检索会飘、会超时。最终采用“规则路+语义路”双路并行。
*   **融合与兜底的逆转**：当两路结果冲突时（如规则路判L3，语义路判L1），通用RAG的“投票取共识”会判为L2，导致漏判。文章采用“取严取高”，直接判为L1。当双路都失败时，通用RAG的“转人工”会延误5-30分钟，文章改为“默认L1”，先弹120。
*   **推荐逻辑的陷阱**：在推荐医生时，若简单加权排序，可能会将“会员妈妈看了三年、离家3公里”的医生排在“三甲主任、离家25公里”的医生后面，破坏了“懂你、连续”的核心卖点。解决方案是使用“置顶规则”而非“加分项”。

### 实践建议（200字）

1.  **场景先行，技术后置**：在应用任何技术前，必须清晰定义“输错代价”。对于“做错会死人、会赔钱”的高风险产品，必须彻底重构通用方案，不能照搬。
2.  **以“最坏情况”为设计原点**：在融合、兜底等关键决策点，假设所有环节都可能出错，将兜底策略设定在“最安全”的一端，宁可误报，绝不漏报。
3.  **构建对抗性测试集**：评测集不能仅依赖历史数据，必须由领域专家（如急诊医生）构造覆盖罕见但致命场景的“对抗性案例”，并用目标用户的“口语化”表达来书写。
4.  **区分“安全决策”与“体验决策”**：在分诊等安全决策上，采用“取严取高”的零容忍策略；在推荐等体验决策上，可以回归“找最优、可平衡”的逻辑，但需用硬约束过滤和业务规则置顶来保证核心体验。
5.  **数据标签需专家重审**：历史工单的结论不能直接作为标准答案，必须由领域专家重新标注，避免将历史错误带入新模型。

### 关键词标签

高风险产品设计，RAG，医疗分诊，零漏判，取严取高，对抗性测试，业务规则置顶，输错代价

---

*本文由AI自动总结生成，仅供参考。*
