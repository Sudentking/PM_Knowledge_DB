# AI医疗问答项目系列之意图识别

**作者**: 
**发布时间**: 
**原文链接**: [https://www.woshipm.com/ai/6365692.html](https://www.woshipm.com/ai/6365692.html)
**标签**: 

---

好的，作为一名专业的产品经理知识总结专家，我将对您提供的文章进行详细总结。

---

## 核心观点（200字）

本文核心观点是：在AI医疗问答产品中，**意图识别是决定系统合规性、准确性与成本效率的关键前置环节**。作者提出，不应让所有用户问题都直接交由大模型处理，而应通过一套轻量级、多层次的“三级意图体系”进行前置分类与过滤。这套体系的核心价值在于：第一，作为**合规的第一道防线**，自动拦截诊断、开药等高风险咨询；第二，实现**问题类型的自动化分类**，替代人工判断；第三，通过**区分热门与冷门意图**，实现“80%问题不走大模型”的成本优化策略，仅让20%的复杂、小众问题调用大模型。最终，意图识别成为连接用户问题与不同处理策略（FAQ、小模型、RAG+大模型）的智能“开关”，是医疗问答产品实现高效、合规、低成本运营的基石。

## 方法论/框架（300字）

文章提出了一套可复用的 **“三级意图识别体系”** 框架，具体如下：

1.  **一级意图（规则引擎层）**：
    *   **目标**：实现80%问题的快速、低成本处理，并完成合规拦截。
    *   **方法**：采用**关键词+正则表达式**的规则引擎。
    *   **功能**：
        *   **违规拦截**：识别并直接拒绝包含“诊断”、“开药”、“胸痛”、“大出血”等高风险词的意图。
        *   **热门意图匹配**：快速匹配“胎动”、“挂什么科”等高频、标准化的意图。
        *   **大方向分类**：将问题粗分为“健康科普”、“就医引导”、“用药禁忌科普”等一级类别。

2.  **二级意图（轻量模型层）**：
    *   **目标**：处理规则引擎无法覆盖的剩余20%问题，进行精细化分类。
    *   **方法**：使用**医疗领域轻量级小模型**（7B以内，可本地部署）。
    *   **功能**：为问题打上精确的“意图标签”，如“孕期护理-胎动相关”、“报告解读-血常规”。该模型**只做分类，不生成回答**，极大节省成本。

3.  **三级意图（频次统计层）**：
    *   **目标**：动态判断问题的热门/小众程度，以决定后续处理路径。
    *   **方法**：后端统计近30天内同一意图或问句的出现频次。
    *   **功能**：频次≥阈值的标记为“热门”，走FAQ或小模型；频次＜阈值的标记为“小众”，走RAG+大模型。

**流程位置**：该意图识别体系被置于整个问答流程的**最前端、最核心**位置，作为所有问题的入口过滤器。

## 关键案例（200字）

文章通过一个典型案例生动展示了意图识别的必要性：

*   **用户问题**：“胎动少”
    *   **无意图识别**：系统可能直接将其归类为“孕期护理科普”，给出标准答案。
    *   **有意图识别**：系统会识别出这是一个**风险咨询意图**，因为“胎动少”可能暗示胎儿缺氧。此时，意图识别会将其拦截，并**直接引导用户就医**，而非进行科普。这体现了意图识别在保障用户安全、规避产品风险上的关键作用。

另一个案例说明了意图识别对成本优化的价值：

*   **同一问题，不同意图**：
    *   “胎动少” → **意图：孕期护理科普（热门）** → 走FAQ或小模型，成本极低。
    *   “我胎动少是不是胎儿缺氧” → **意图：风险咨询（小众/复杂）** → 直接拦截或引导就医，或走RAG+大模型进行深度分析。
    *   这个案例清晰地展示了，没有意图识别，系统无法区分这两种本质不同的需求，要么全部走大模型导致成本高昂，要么全部走FAQ导致风险失控。

## 实践建议（200字）

1.  **轻量起步，快速验证**：不必一开始就追求复杂的算法。先用**关键词+正则**搭建一级意图识别，覆盖80%的常见场景，尤其是合规拦截和热门问题。这足以快速上线并验证核心逻辑。

2.  **小模型是成本优化的关键**：对于二级意图分类，优先选择**医疗领域的轻量级小模型**（如7B参数以下）。将其部署在本地，只做分类任务，能显著降低对云端大模型的依赖和调用成本。

3.  **动态调整，持续迭代**：三级意图的“热门/小众”阈值不应是固定的。应根据产品上线后的实际数据，**定期（如每月）调整频次统计的阈值**，使分类更贴合用户真实行为。

4.  **与用户分层结合**：将意图识别与用户画像（新/老用户）结合。例如，对新用户提出的“基础意图”问题，回答应更详细并附带强引导；对老用户提出的“明确意图”问题，回答应更精简直接，提升效率。

5.  **建立监控体系**：持续监控一级意图的拦截率、二级意图的分类准确率、以及三级意图的分布变化，及时发现并修复规则或模型的漏洞。

## 关键词标签

意图识别，医疗问答，三级意图体系，规则引擎，轻量模型，成本优化，合规拦截，产品设计

---

*本文由AI自动总结生成，仅供参考。*
