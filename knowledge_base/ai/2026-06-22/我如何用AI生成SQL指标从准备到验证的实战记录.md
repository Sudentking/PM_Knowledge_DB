# 我如何用AI生成SQL指标：从准备到验证的实战记录

**作者**: 
**发布时间**: 
**原文链接**: [https://www.woshipm.com/ai/6416541.html](https://www.woshipm.com/ai/6416541.html)
**标签**: 

---

好的，作为一名专业的产品经理知识总结专家，我将对您提供的文章进行详细总结。

---

## 核心观点（200字）

本文核心观点是：AI不仅能，而且应该被用于生成SQL指标代码，以解放数据开发人员的手工劳动。作者通过亲身实践，挑战了“数据部门无法被AI替代”的固有认知。文章指出，AI生成SQL的成功关键在于**前期准备**，而非与AI的对话本身。这个过程倒逼企业进行数据基建的规范化与标准化（如指标字典、元数据、血缘关系），这是一项边际成本递减、效率不断凸显的投资。AI的价值不在于完全替代人，而在于将人力从重复、低效的“手搓代码”中解放出来，去从事更高价值的数据分析和业务洞察工作。作者鼓励产品经理和数据从业者主动拥抱AI，成为推动事情发生的人。

## 方法论/框架（300字）

文章提出了一套完整的**基于大模型的SQL代码自动化生成方案**，其核心框架分为四个阶段：

1.  **准备阶段（核心）**：这是最耗时但最关键的一步。需要准备两类文档：
    *   **指标字典**：包含指标计算口径、计算因子对应表、字段的血缘关系。这是AI理解业务逻辑的基础。
    *   **元数据信息**：包含字段名称、字段说明、数据样本。**注意**：元数据注释需保持纯粹，避免加入“值说明”等非结构化信息，以免干扰AI的注意力权重。

2.  **选型与设定阶段**：
    *   **选型**：通过向AI提问“我需要提供哪些必要信息”，让AI反向输出方案，并选择最简洁、清晰的方案（文中选择了Claude）。
    *   **设定目标与边界**：明确AI生成SQL的最终目标是输出**唯一准确的具体数值**。因此，选择口径复杂的已开发指标作为验证案例，通过对比AI生成与人工手搓的SQL结果来验证准确性。

3.  **执行与验证阶段**：
    *   **首次生成**：将准备好的文档输入AI，生成SQL代码。
    *   **排查与迭代**：对比AI生成与人工代码的执行结果。发现偏差后，分析原因（如元数据中的“值说明”干扰），调整提示词，重新生成。
    *   **最终验证**：执行两段SQL，对比结果。如果存在小数位偏差，可将两段代码交给AI进行差异分析，并辅以人工二次计算校验，确认更优的逻辑。

4.  **沉淀与复用阶段**：
    *   **保存成功经验**：将一次有效的执行过程（包括提示词、文档、结果）保存下来。
    *   **创建Skills**：在AI工具中，将上述过程创建为“技能”（Skills），以便后续对话中通过“/技能名称”直接调用。
    *   **生成文件**：将整个执行过程生成一个文件，下次对话时让AI访问该文件，学习上次的成功经验。

## 关键案例（200字）

文章的核心案例是作者亲测AI生成一个口径复杂的业务指标SQL。

*   **背景**：作者所在公司数据部门仍坚持“手搓代码”，认为与AI沟通的时间成本高于自己编写。
*   **行动**：作者决定自己尝试。他准备了指标字典和元数据文档（包含字段名称、说明、数据样本），并选择了Claude作为AI工具。
*   **问题与解决**：首次生成的SQL中，`WHERE`条件多了三个未在指标字典中声明的条件。排查发现，原因是元数据文档中，这三个字段恰好是唯一进行了“值说明”注释的字段，干扰了AI的注意力。作者调整提示词后，得到了正确的代码。
*   **结果与验证**：执行AI生成与人工手搓的SQL，发现结果存在小数位偏差。将两段代码交给Claude分析，AI快速定位到差异：人工代码是按SKU单独算天数再取整，而AI代码是先汇总所有金额再统一算天数。经过人工二次计算校验，确认AI生成的计算逻辑更准确。**首战告捷，验证了AI生成SQL的可行性与优越性。**

## 实践建议（200字）

1.  **主动拥抱，而非被动防御**：不要因为“AI还替代不了我”而沾沾自喜。应主动思考如何利用AI辅助工作，探索新的可能性，而不是等待被颠覆。
2.  **忘掉岗位属性，先干起来**：数据产品经理等非纯开发岗位，可以凭借对业务和SQL的理解，率先尝试AI生成SQL的路径，成为“破局者”。
3.  **重视前期准备，而非对话本身**：AI生成SQL的成功率，90%取决于前期准备的指标字典、元数据等文档质量。花时间做好数据基建，是边际成本递减的高效投资。
4.  **设定明确目标与验证边界**：对于数据指标这种要求唯一准确数值的任务，必须设定严格的验证标准。选择已开发的口径复杂指标作为测试案例，通过结果对比来验证AI代码的准确性。
5.  **保持元数据注释的纯粹性**：在准备元数据文档时，避免在字段说明中加入“值说明”等非结构化信息，以免干扰AI的注意力权重，导致生成错误的过滤条件。
6.  **沉淀成功经验，实现复用**：将一次成功的AI生成过程（包括提示词、文档、结果）保存下来，创建为“技能”或文件，以便后续快速复用，实现效率的指数级提升。

## 关键词标签

AI生成SQL, 数据产品, 指标字典, 数据基建, 元数据, 提示词工程, 边际成本递减, 概率性思维

---

*本文由AI自动总结生成，仅供参考。*
